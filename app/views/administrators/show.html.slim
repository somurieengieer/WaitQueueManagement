p#notice
  = notice
p
  strong
    | Your Name:
  = @administrator.name
p
  strong
    | Your Mail:
  = @administrator.mail
= link_to 'Edit', edit_administrator_path(@administrator)
|  | 
= link_to 'Back', administrators_path
|  | 
= link_to 'Destroy', @administrator, method: :delete, data: { confirm: 'Are you sure?' }

table.table.table-striped.mt-3
  thead
    tr
      th
        | Wait queue's name
      th
        | Current order
  tbody
    - @wait_queues.each do | wait_queue|
      tr
        td
          = link_to wait_queue.name, wait_queue
          /= wait_queue.name
        td
          = wait_queue.count
= link_to 'Create New "Wait Queue"', new_wait_queue_path
